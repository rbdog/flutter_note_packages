name: mobile_preview
version: 0.0.1
description: A temporary alternative to mobile_preview. (https://pub.dev/packages/mobile_preview)
homepage: https://github.com/rbdog/flutter_note_packages

environment:
  sdk: ^3.0.0
  flutter: ^3.16.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  freezed_annotation:
  json_annotation:
  shared_preferences:
  collection:
  flutter_riverpod:
  flutter_hooks:

dev_dependencies:
  flutter_lints:
  build_runner:
  freezed:
  json_serializable:
